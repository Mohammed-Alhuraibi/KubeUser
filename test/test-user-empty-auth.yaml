apiVersion: auth.openkube.io/v1alpha1
kind: User
metadata:
  name: empty-auth-user
spec:
  # auth section is empty/missing - will default to x509 with default TTL (90 days)
  # This is ALLOWED and will be automatically set to x509
  clusterRoles:
    - existingClusterRole: view
