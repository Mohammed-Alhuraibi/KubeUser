apiVersion: auth.openkube.io/v1alpha1
kind: User
metadata:
  name: unsafe-floor-user
spec:
  auth:
    type: x509
    ttl: "24h"
    renewBefore: "23h58m"  # WILL BE REJECTED: Leaves only 2 minutes, violates 5-minute safety floor
    autoRenew: true
  clusterRoles:
    - existingClusterRole: view
